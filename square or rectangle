import java.util.Scanner;

public class Main
{
	public static void main(String[] args) {
		Scanner s=new Scanner(System.in);
		int a=s.nextInt();
		int b=s.nextInt();
		int c=s.nextInt();
		int d = s.nextInt();
		if(a==b&&b==c && a==c&&c==a&&a==d&&d==b&&c==d){
		    System.out.println("these sides form a square");
		}
		else if(a==b && c==d)
		{
		    System.out.println("these sides form a rectangle");
		}
		else{
		    System.out.println("they dont form both");
		}
	}
}
